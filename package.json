
{
  "name": "implicit-growth-calculator",
  "version": "1.0.0",
  "description": "An application to calculate the implicit growth rate of a stock's price.",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^1.9.0",
    "@netlify/functions": "^4.1.10"
  }
}